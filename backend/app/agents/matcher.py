# Agente A1 — Matching de Código.
# Dado el código de un repo y las tecnologías objetivo del proyecto, identifica
# los fragmentos con mayor potencial de traducción (Fase 2).


async def analizar_repo(codigo_por_archivo: dict[str, str], tecnologias: list[str]) -> list[dict]:
    raise NotImplementedError
